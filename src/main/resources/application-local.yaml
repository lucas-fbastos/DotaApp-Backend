spring:
  r2dbc:
    url: r2dbc:postgresql://localhost/dotacodex
    username: root
    password: root
management:
  endpoints:
    web:
      exposure:
        include:
          - "health"
          - "info"
  endpoint:
    info:
      enabled: true